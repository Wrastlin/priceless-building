# Motion — failed attempts archive (labeled, never deleted)

Central archive hub for motion-production work that FAILED or was SUPERSEDED. Nothing here is
production; nothing here should be shipped or reused as-is. Kept per the never-delete law so the
lessons stay checkable. The lessons themselves live in `docs/motion/MASTER-EXECUTION-PLAN.md`
(rules 7a, 7b, rail law) and agent memory `priceless-motion-design-standards`.

## 2026-07-23-unstable-camera-takes/ (18 files)
Every camera-travel clip generated by PROMPT-DESCRIBED motion — all exhibit walking bob,
footsteps, lurching pauses, or handheld wobble. Aaron rejected them ("terribly unstable...
having a seizure"). Includes the five door-aisle grammar experiments (dolly / gimbal rig /
cinematic tour / commercial preview / pan-in) — all five failed; words cannot stop kling from
walking a travel shot. REPLACED BY: the rail method (start-image + end-image), locked as law.
The stable replacements live in `docs/motion/renders/motion-clips/` and shipped to
`PRICE-LESS DELIVERABLES/12 Motion Clips/`.

## 2026-07-23-stability-comparisons/ (12 files, two copies)
The A/B/C/D/E test files used to diagnose choppiness and judge fixes (original 24fps vs
interpolated vs deshake vs rail). Purpose served: Aaron picked the rail method. Archived so the
deliverables folder holds only production material.

## Also archived conceptually (left in place for technical reasons)
- `public/ambient/` in the app repo: the REJECTED homepage ambient-video loops (2026-07-23,
  built from pre-rail choppy clips). Unused by any page since the rollback commit 88f60ce.
  Left in the repo because the website is currently locked ("not touching website"); fold into
  an archive commit at the next approved site pass.

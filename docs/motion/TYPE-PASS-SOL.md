# Type Pass — Sol

**Date:** 2026-07-22  
**Composition:** `ROOF-silent-v3`, 1080 × 1920

## Final metrics

- Italic floor: 58px, weight 300 italic, `0.02em` tracking, 34px gap to hero
- Display bold: 210px, weight 700
- PunchClose: `FOR` / `LESS`, two lines, `-0.02em` tracking, `0.86` line height
- Skinny floor: 30px, weight 300, `0.28em` tracking, 44px gap after hero
- Stack max width: 936px
- Punch side padding: 72px
- Punch scale: 1.0
- Optical placement: stack-level `translateY(-3%)`
- Entrance: one-frame hard reveal and hold; no close slam

## Iteration notes

The first still used `Quality` at 44px above a single-line `FOR LESS` at 124px. It was clean but still read as a restrained caption stack rather than type owning the void.

The final pass incorporates the exact display recommendations from `TYPE-CRITIQUE-OPUS.md`: `Quality` is removed; the hero is a two-line 210px block; max width is 936px; side padding is 72px; bold tracking is `-0.02em`; line height is `0.86`; and the close uses a one-frame hard reveal with no slam. The final still confirms the phrase occupies roughly 39% of frame width and reads as the graphic.

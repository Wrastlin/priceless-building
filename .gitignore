# dependencies
node_modules

# next.js
.next
out

# env — ignore every real env file; keep only the *.example templates
.env*
!.env.example
!.env.cloud.example

# build / cache
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# research scratch (not for git, internal notes only)
.research

# raw walkthrough footage + extracted frames (too big for git)
footage/

# sandbox demo build (python venv, generated catalog, frames, clips, video) — not for prod
sandbox/
public/sandbox/
_archive/lab-experiments-2026-06-14/

# file-backed item store (dev source of truth; prod moves to Supabase)
data/
data/items.json

# editor / OS
.DS_Store
.vscode
.idea
*.swp

# logs
*.log
npm-debug.log*

# playwright test artifacts
.playwright-mcp/

# DEV-ONLY admin UI preview — must never reach production. Also 404s in prod
# via a NODE_ENV guard and is listed in .vercelignore. Safe to delete anytime.
app/dev-preview/

# Photo scrape inbox (Apify/IG dumps) — curate keepers out; don't ship raw pulls
public/real-photos/_inbox/

# local product photo test fixtures
../test-photos/
docs/intake-batches/**/items-backup-full-*.json.gz

# motion pipeline: source is tracked, heavy output is not.
# These render outputs are NOT backed up by git. See docs/motion/BACKUP.md
motion-remotion/node_modules/
motion-remotion/out/
motion-remotion/public/
docs/motion/renders/
docs/motion/youtube-pipeline-sources/

import { AbsoluteFill } from "remotion";

/** Unused blank — compositions live in Root.tsx folders. */
export const MyComposition = () => null;

export const MyComponent = () => (
  <AbsoluteFill style={{ backgroundColor: "#f7f5f1" }} />
);
